<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="./css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700;800;900&family=Rubik+Mono+One&display=swap" rel="stylesheet">
    <title>Collection</title>
</head>
<body>
    <header>
        <%- include('./partials/nav.ejs', { loggedIn: loggedIn }) %>
    </header>

    <div class="collection_container" id="collectionContainer">
        <h1>Collection</h1>
            <% cars.forEach(car => { %>
                <div class="collection_post">
                    <a href="/detailsCar/<%= car._id %>"> <!-- Add link to car details page -->
                        <img src="<%= car.imagePaths[0] %>" alt="<%= car.make %> <%= car.model %>">
                        <!-- Use the basename function to get just the filename from the full path -->
                        <h3><%= car.make %> <%= car.model %></h3>
                        <ul>
                            <li>Year: <%= car.year %></li>
                            <li>Mileage: <%= car.mileage %></li>
                            <li>Time to Auction: <span id="auctionTime_<%= car.model %>"></span></li>
                            <li>Starting Auction Price: $<%= car.startingPrice %></li>
                        </ul>
                    </a>
                </div>
            <% }) %>
    </div>
     <!-- <script src="./js/scripts.js"></script> -->
</body>
</html>
